id: "coding-005"
name: "添加错误处理"
description: "Agent 应为 parse_config 添加适当的错误处理"
domain: coding
task: |
  改进 src/lib.rs 中的 parse_config 函数：
  1. 将返回类型改为 `Result<HashMap<String, String>, String>`
  2. 对空输入返回错误
  3. 对格式错误的行（没有 = 分隔符）跳过并记录警告
  4. 更新所有相关测试
  确保 `cargo test` 全部通过。
project_fixture: "fixtures/rust-project"
success_criteria:
  all_of:
    - test_pass:
        command: "cargo test"
    - tool_used: "edit_file"
    - output_contains: "Result"
constraints:
  max_files_changed: 2
  required_read_before_edit: true
